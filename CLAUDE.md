# プロジェクト概要

Playwrightを用いたブラウザアプリのテスト自動化を学ぶための学習プロジェクト。題材として、ユーザー情報入力フォーム（テキストボックス・スライダー・プルダウン・ラジオボタン・ボタン等のUI一式を持つ）を、フロントエンドHTML/CSS/JavaScript・バックエンドC#(.NET 10)で開発し、そのUIに対するPlaywrightテスト（さまざまな入力値と期待値の組み合わせを扱う）を書く。

## レビュー方式

**自律ループモード**を採用する。オーケストレーター（メイン会話）が開発サブエージェントとレビューサブエージェントを`Agent`ツールで独立に呼び出し、レビュー指摘がゼロになるまで自動で修正・再レビューを繰り返す。詳細は`waterfall-dev-workflow`スキルの「自律ループモード」章・`references/loop_mode.md`を参照。

セットアップ時にユーザーと合意した承認チェックリスト（2026-09-23、`docs/qa_log.md`参照）:

- **再レビューの上限回数**: 5回。上限到達時、残存指摘が全て軽微なら既定通り自動的に次工程へ進める（`docs/review_log.md`に経緯を記録）。重大な指摘が残っている場合は自動進行を止め、ユーザーに判断を仰ぐ。
- **「指摘ゼロ＝工程自動移行」の適用範囲**: 節目のみ人間確認とする。要件定義→コンポーネント設計→関数設計→実装の各工程間移行は、レビューサブエージェントの指摘ゼロ判定のみで自動的に進めてよい。ただし**テスト工程完了時**（＝実装が一通り完成し動作確認まで済んだ時点）は、次の公開工程（GitHub公開）に進む前に、ユーザー自身が成果物を確認してから明示的に承認する。
- **進捗通知**: 開発・レビューサブエージェントの往復ごとの通知は不要。1つの工程の作業時間が2時間を超えた場合にのみ、報告のため作業を止める（経過時間は当該工程の最初のコミット時刻から起算）。
- **Git運用・禁止操作・コミット規約**: 変更なし（下記「Gitコミット規約」章、`git-conventions`スキルに従う）。

`docs/qa_log.md`（人間の判断記録）と`docs/review_log.md`（自動レビュー往復の記録）を役割分担して運用する。

## 開発プロセス

以下の順に工程を進める。要件定義〜実装の工程間移行はレビューサブエージェントの指摘ゼロ判定で自動的に進めてよいが、**テスト工程完了→公開工程（GitHub公開）への移行のみ、ユーザーの明示的承認を得る**（上記「レビュー方式」参照）。

1. 要件定義 (`docs/01_requirements/`)
2. コンポーネント設計 (`docs/02_component_design/`)
3. 関数設計 (`docs/03_function_design/`)
4. 実装 (`src/`)
5. テスト (`docs/04_test/` に仕様書、`tests/` にテストコード)。テストレベル定義・由来管理・リグレッション方針など戦略の詳細は `test-strategy` スキルに従う（「テスト戦略」章参照）
6. 公開 (`docs/05_publication/`) — GitHubへのPublicリポジトリとしてのソースコード公開のみを指す。サーバーへの配置など、アプリを実行可能な状態で公開することは行わない（下記「公開（GitHubへのソースコード公開）」章参照）

各工程の成果物には、要件定義書のID（REQ-xx / NFR-xx / CON-xx）を起点としたIDを付与し、`docs/traceability_matrix.md` のトレーサビリティマトリックスに追記すること（詳細は「トレーサビリティマトリックス」章を参照）。

## 現在の進捗

- [x] プロジェクトセットアップ（CLAUDE.md・フォルダ構成・.claude/settings.json）
- [x] .NET 10 SDK導入（2026-09-23、ユーザーが導入完了。10.0.401）
- [x] 要件定義（`docs/01_requirements/requirements.md` v1.4、2026-09-23完了。REQ-01〜12/NFR-01〜08/CON-01〜07。NFR-02はCOMP-02/05の関数設計工程で判明した解釈曖昧さの解消のためv1.3/v1.4で改訂済み）
- [x] コンポーネント設計（`docs/02_component_design/component_design.md` v1.3、2026-09-23完了。COMP-01〜06）
- [x] 関数設計（COMP-01〜06完了。COMP-06の可読性向上フェーズはユーザー指示〈レビュー1回制限〉により保留、`docs/review_log.md`参照）
- [x] 実装（2026-09-23、`src/`・`tests/`・`docs/tutorial/`。実装レビュー1回、指摘12件対応済み）
- [x] テスト（2026-09-23、自動テスト81件全件成功。ユーザー確認・公開工程への移行承認済み）
- [ ] 公開（GitHubへのソースコード公開、Public）— 履歴修正済み、GitHubリポジトリ`learn-playwright-dotnet`作成済み、**push承認待ち**（`docs/05_publication/publication_manual.md`参照）

## 確定した仕様

- 対応OS: Windows 11（ローカル実行のみを前提とする。セキュリティ対策を最重要項目として扱う）
- フロントエンド: HTML / CSS / JavaScript
- バックエンド: C# / .NET 10（ASP.NET Coreを想定。具体的な構成はコンポーネント設計工程で確定する）
- UIコンポーネント: テキストボックス、スライダー、プルダウンリスト、ラジオボタン、ボタン
- データフロー: UIで入力した情報を送信ボタンでPOSTし、サーバー側でJSONとして保存する。読み込みボタンでGETし、受け取ったJSONの内容でUIの見た目を復元する
- ユーザー識別: Cookieを用いる
- テストツール: Playwright for .NET（C#、NUnitベース）。バックエンドと同じ言語でテストを書ける点を優先し、Node.js/TypeScript版のPlaywright Testは採用しない（2026-09-23、`docs/qa_log.md`参照）
- テストの主眼: UIへの多様な入力値と期待値の組み合わせを扱いやすくする便利関数・データ構造（テストケースのパラメータ化等）を重視して整備する
- 公開: GitHubにPublicリポジトリとして公開する。pushのたびに機密情報・個人情報スキャンを行う（下記「公開（GitHubへのソースコード公開）」章参照）

## 実装環境

- .NET SDK: `8.0.408`・`9.0.314`・`10.0.401`が導入済み（`C:\Program Files\dotnet\sdk`）。`global.json`で.NET 10系（`10.0.100`以上、rollForward: latestFeature＝導入済みの最新10.0系SDK）に固定済み（教材読者の環境差を吸収するため。`docs/review_log.md`参照）。
- 複数SDKが混在する環境のため、`global.json`でSDKを.NET 10系に固定している（上記）。各プロジェクトファイル（`.csproj`）の `TargetFramework` は `net10.0` を明記している。
- Node.js / npm: 未導入（不要）。Playwright用Chromiumは2026-09-23に導入済み（`%LOCALAPPDATA%\ms-playwright`、ユーザー許可の上でClaudeが実行）。2026-09-23の導入はユーザーから「今回に限り」許可を得てClaudeが実行したもの。**Chromiumの再導入・更新（Playwrightパッケージ更新時を含む）はユーザー自身が行うか、その都度ユーザーの承認を得ること（常設の許可ではない）**。参考: このPCにはPowerShell 7（pwsh）が無いため、Windows PowerShellで`powershell -ExecutionPolicy Bypass -File tests/LearnPlaywright.Tests/bin/Debug/net10.0/playwright.ps1 install chromium`とする（Bypassは当該プロセスのみ）。
- 標準コマンド（`.claude/settings.json`の`permissions.allow`に登録済み）: ビルド`dotnet build`、全テスト`dotnet test`、サンプルサイト起動`dotnet run --project src/LearnPlaywright.Server`（http://localhost:5080/ ）。E2Eテストはサーバーを自動起動するため、手動起動したサーバーは停止してから`dotnet test`を実行する（ポート使用中を検出するとE2Eは失敗する）。

## トレーサビリティマトリックス

要件から実装・テストまでの対応関係を追跡できるよう、`docs/traceability_matrix.md` を工程が進むたびに追記・更新する（工程ごとに別ファイルを作らず、1ファイルで育てていく）。

- 要件↔実装↔テストを1行にまとめた単一表ではなく、**隣接する工程どうしの対応表を4つ**用意する方式とする: ①要件×コンポーネント、②コンポーネント×関数、③関数×テスト、④要件×テスト（直接検証トレース）
  - 理由: 1要件が複数コンポーネント/関数/テストに対応する場合や、逆に1つの関数・テストが複数要件にまたがる場合（多対多）があるため
- 各対応表は、**上流工程のIDを行、下流工程のIDを列に配置したマトリクス形式**とし、関連がある交点に「○」を付ける（多対多の関係を一覧性高く表現するため）
- ③④は関数・テストケースの件数が多くなりやすく列が肥大化するため、列は個々のテストIDではなく**テストモジュール（テストファイル）ID**とし、セルには該当する具体的なテストIDを複数列挙してよい（○ではなくID表記。例: `TEST-03, TEST-07`）
- マトリクスのセルには「○」またはテストIDの列挙のみを記載し、**対応関係の具体的な根拠（なぜ関連するか）はマトリクスに書かず、各工程の成果物（コンポーネント設計書・関数設計書・テスト仕様書）側にID単位で明記する**
- ある行（ID）に対応する列が1つも無い場合（該当する下流要素が存在しない等）は、そのままセルを空欄にしてよい。理由の補足が必要な場合は、表の下に簡潔な注記を添える程度にとどめる
- テスト工程完了時には、「④要件×テスト対応表」の全要件ID（REQ/NFR/CON）の行に最低1つのテストIDが記載されていることを確認し、テストの網羅性チェックとすること
- マトリックスの更新自体は「変更操作の承認について」に従い、通常のドキュメント編集としてその都度の承認確認は不要

### ID対応関係の一次情報源・記載方針（可読性）

要件ID・コンポーネントID・関数ID・テストIDの対応関係を、`docs/traceability_matrix.md` と各成果物本文・テストコードの複数箇所に重複して手で記載すると、一方を直しても他方への反映漏れが構造的に発生し、レビューの往復・トークン消費を押し上げる。これを避けるため、以下を徹底する。

- **一次情報源（single source of truth）は `docs/traceability_matrix.md` のみとする**。各工程の成果物本文には、対応するIDの**簡潔な一覧**（カンマ区切り等）のみを記載してよい。
- 対応関係が自明でないID についてのみ、箇条書きで理由を個別に補足する。IDを列挙し1つ1つに逐語的な括弧書き説明を付けて1文に詰め込む書き方は避ける。
- **テストコード（`tests/`）のコメント等には、対応するテストID（およびごく短い検証内容の要約）のみを記載し、要件ID・関数IDのフル一覧は重複記載しない**。
- アーキテクチャ図・データフローは、複雑になる場合はMermaid（`flowchart` / `sequenceDiagram`等）による図示を検討する。
- 各設計書の冒頭に、版数・作成日・改訂履歴を記載する「文書情報」表を置くと、レビュー指摘への対応履歴が追跡しやすい（任意だが推奨）。

## 品質方針（テスト容易性）

- ロジック（入力値の検証・JSONへの変換・復元処理など）とUI/表示処理・HTTP通信処理を分離し、ロジック層は外部ライブラリに依存せず単体テスト可能な構成にすること
- 関数・メソッドは可能な限り入力と出力（戻り値）が明確な単位に分割し、副作用（DOM操作・ファイルI/O・グローバル状態変更など）とロジックを混在させないこと
- 関数設計書には、各関数の入出力仕様をテストケース設計に転用できる粒度で記載すること
- テスト仕様書（`docs/04_test/`）の各テストケースは、対応する関数設計ID・要件IDとともにトレーサビリティマトリックスに反映すること

## テスト戦略

テストレベル定義（単体/結合/統合の3階層と各レベルでの機能要件・非機能要件の検証観点）、テストの由来管理（仕様/設計ベース・コードベース・レビュー指摘ベース）、カバレッジ・網羅性の考え方、リグレッション方針、テスト実行・ツール運用の工夫、TDD/テスト作成タイミングの方針は `test-strategy` スキルに従う。

テスト仕様書（`docs/04_test/`）のテストケース一覧には、テストID・対応要件ID・対応関数IDに加えて**テストレベル**（単体/結合/統合）と**由来**（仕様/設計ベース・コードベース・レビュー指摘ベース）を列として持たせること。由来が「仕様/設計ベース」のテストのみトレーサビリティマトリックスの記載対象とする。

- **Playwright UIテストの入力値パラメータ化**: テキストボックス・スライダー・プルダウン・ラジオボタンの組み合わせを網羅的に試すため、NUnitの`TestCaseSource`等を使い、入力値と期待値の組をデータ構造（レコード/クラス）として一元管理する。個々のテストメソッドに値をハードコードしない。詳細な設計は関数設計・テスト仕様書工程で確定する。
- リグレッション方針: フルリグレッション（`dotnet test`で全件、所要1分未満）。TDD不採用（設計書ベースの後付けテスト）。代表ケースのマーキングは不要。詳細は`docs/04_test/test_spec.md`。

## 変更操作の承認について

プロジェクトフォルダ内の変更はGitで追跡・復元可能なため、以下の場合は事前の承認確認を省略してよい。

- 対象: ドキュメント・ソースコードの編集など、Gitで追跡可能な個別のファイル変更操作
- 対象: 上記の変更を確定させる `git commit`（Gitで追跡・復元可能なコミットは都度の承認確認なしに実行してよい。コミットメッセージ規約は「Gitコミット規約」章に従うこと）
- 条件: ユーザーがその場で明示的に承認確認を求めていない場合

ただし、以下は引き続きユーザーの明示的な承認を得てから行うこと（対象外）。

- 「開発プロセス」章に定めるテスト工程完了→公開工程への移行（節目のゲート承認）
- Gitで追跡されない・復元不能な操作（`git push`、ファイル削除、外部システムへの送信など）

### push運用方針

本プロジェクトでは **`git push`は都度確認する**（2026-09-23、ユーザーと合意、`docs/qa_log.md`参照）。自律ループ実行中は確認相手が不在のためpushを行わずコミットのみに留め、ユーザーがループを止めた・確認しに来たタイミングでまとめてpush可否を確認する（`waterfall-dev-workflow`スキル`references/loop_mode.md`「工程間ゲートの扱い」章の運用に従う）。

## 公開（GitHubへのソースコード公開）

本プロジェクトの「公開工程」は、サーバーへの配置・実行可能ファイル化等ではなく**ソースコード一式をGitHubにPublicリポジトリとして公開すること**を指す。Publicかつユーザー個人の環境（機微な情報も扱う個人の開発PC）で開発する前提のため、pushのたびに以下を徹底する。

1. **push前の機密情報・個人情報スキャン（毎回・省略不可）**
   - 追跡対象ファイルの内容: APIキー・パスワード・接続文字列・個人を特定しうる情報（実名・メールアドレス・電話番号等）が含まれていないか、差分（`git diff`/`git show`）とリポジトリ全体の両方の観点で確認する
   - **コミット履歴のメタデータ**: `git log`のauthor名・emailに実名・個人メールアドレスが残っていないか確認する（グローバルCLAUDE.mdの方針。内容スキャンだけでは見逃すため）。Gitの`user.name`/`user.email`設定は`git-conventions`スキルの規約（コミット者ID固定値）に従っていることを前提とする
   - 疑わしい内容が見つかった場合は、push前に必ずユーザーに報告し、除去・修正してから進める
   - スキャンを実施した旨と結果は、`docs/qa_log.md`に「Pushスキャン記録」として都度追記する（書式は`docs/qa_log.md`冒頭のテンプレート参照）
2. **push実行の承認**: 上記スキャン結果を提示したうえで、push実行そのものをユーザーに確認してから行う（「push運用方針」章の通り、都度確認）
3. リポジトリ名・公開範囲（Public）・ライセンス（学習用サンプルのため選定要）は、テスト工程完了後の公開工程の着手時にユーザーと合意し`docs/qa_log.md`に記録する
4. 手順の詳細は`docs/05_publication/`にマニュアルとして作成する（テスト工程完了後）

### 厳格に禁止するGit操作

以下はユーザーの承認の有無にかかわらず、Claudeは実行しないこと（承認確認をして良いかを尋ねることもしない。ユーザーから明示的に依頼された場合も、まず本方針がある旨を伝えて確認を仰ぐこと）。

- マージ系コマンド（`git merge` など）
- 過去のコミットを書き換えるコマンド（`git commit --amend`、`git rebase`、`git filter-branch`、`git push --force` / `--force-with-lease` / `-f`、`git reset --hard` など）

`.claude/settings.json` の `permissions.deny` にも上記コマンドを登録し、技術的にも実行をブロックしている。

## プログラム実行の承認について

設計書に基づいて作成したプログラム（`src/`の実装コード、`tests/`のテストコード）を、動作確認・レビューのために実行する操作は、以下のいずれにも該当しない限り、都度の承認確認を省略してよい。

- プロジェクトフォルダ外のファイルを書き換える場合
- 本章より下で、特定の実行操作について承認を要する旨が明記されている場合

実行コマンドは「実装環境」章の標準コマンド（`dotnet build`・`dotnet test`・`dotnet run --project src/LearnPlaywright.Server`）を`.claude/settings.json`の`permissions.allow`に登録済み。

### テスト実行コマンドの標準化（承認プロンプト削減）

- テスト実行は`dotnet test`の標準形のみを使う（プロジェクトファイルへの絶対パス直指定・作業ディレクトリを都度変える`cd`連結は避ける）
- テストの入出力はテキスト形式（`dotnet test`の標準レポート）に統一し、判定は終了コード・テキストレポートのみで行う
- 同一テストを繰り返し実行してflaky挙動を確認する必要がある場合も、シェルのループで包まず標準形を複数回呼び出す

## セッション運用方針

1. **即時着手**: ユーザーから指示を受けたら、作業概要の提示や承認を待たず、プロジェクト内のファイル作成・編集にすぐ着手してよい。ただし、テスト工程完了→公開工程への移行のゲート承認、pushの承認はこの限りでない。
2. **完了時の挙動**: 指示された作業が正常に完了した場合は、完了報告を行った上で待機する（対話セッションを自らの判断で強制終了することはしない）。
3. **申し送り**: ユーザーの明示的承認が必要な事項が残っている場合は、その旨をユーザーに提示するとともに、`docs/handoff.md` に次回セッションへの申し送り事項を記録する。

## 判断・承認のQA記録

Claudeがユーザーに判断や承認を仰ぎ、ユーザーが回答した場合は、`qa-log`スキルに従って `docs/qa_log.md` のQA表に追記すること。プロジェクトルールそのものの見直しが発生した場合も、決定事項として残すこと。push前スキャンの結果は「公開（GitHubへのソースコード公開）」章の通り同ファイルに記録する。

## Gitコミット規約

`git-conventions`スキルに従う（コミット者ID・メッセージ書式・禁止操作・push承認粒度の具体ルールは同スキルを正とし、ここでは重複転記しない）。push承認の粒度は「push運用方針」章の通り**都度確認**。

## 環境メモ

- このPCではPythonが複数バージョン混在しているが（グローバルCLAUDE.md参照）、本プロジェクトはPythonを使用しないため無関係。
- .NET SDKも複数バージョンが混在する（上記「実装環境」章参照）。`global.json`での固定を徹底すること。
- 機微な情報も扱う個人の開発PCであるため（グローバルCLAUDE.md参照）、セキュリティ対策・機密情報の取り扱いには特に注意する。ローカル実行のみを前提とし、認証・認可機構を持たない学習用サンプルであることをREADME等に明記する（本番運用を想定した設計ではない旨の注記）。

## フォルダ構成

```
docs/
  01_requirements/       要件定義書
  02_component_design/   コンポーネント設計書（基本設計）
  03_function_design/    関数設計書（詳細設計）
  04_test/               テスト仕様書（テストケースにテストレベル・由来を付与。test-strategyスキル参照）
  05_publication/         公開手順書（GitHubへのソースコード公開手順。テスト完了後に作成）
  traceability_matrix.md トレーサビリティマトリックス（全工程共通・1ファイル）
  qa_log.md               判断・承認のQA記録（push前スキャン記録を含む）
  review_log.md            レビュー往復の記録（自律ループモード）
  handoff.md               セッション間の申し送り事項（現在アクティブな分のみ）
  handoff_archive.md       （肥大化時のみ）handoff.mdから退避した解決済みの申し送り履歴
src/                      実装コード（`LearnPlaywright.Server`: ASP.NET Core＋wwwroot〈HTML/CSS/JS〉、`LearnPlaywright.Logic`: ロジック層）
tests/                    テストコード（`LearnPlaywright.Tests`: Playwright E2E＋COMP-04ヘルパー、`LearnPlaywright.UnitTests`: 単体テスト）
docs/tutorial/            学習教材（COMP-06、0〜9章）
docs/git_filter_repo_guide/ git filter-repoの解説（examples/は伏せ字版の参照ファイルで公開、private/は伏せ字なしの実物で.gitignore対象）
```
