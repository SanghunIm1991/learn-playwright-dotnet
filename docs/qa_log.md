# QA表

判断・承認のやり取りを記録する。

| 日付 | 工程 | 質問内容 | 回答内容 |
|---|---|---|---|
| 2026-09-23 | セットアップ | 工程ゲートの承認方式はどちらにするか（ユーザーゲート承認方式／自律ループモード） | 自律ループモード |
| 2026-09-23 | セットアップ | GitHubへのgit push（Public公開）はどのタイミングで承認を得るか（都度確認／コミットとセット） | 都度確認 |
| 2026-09-23 | セットアップ | 自律ループモードの承認チェックリスト：再レビューが収束しない場合の上限回数（3回／5回／上限なし） | 5回 |
| 2026-09-23 | セットアップ | 自律ループモードの承認チェックリスト：「指摘ゼロ→工程自動移行」を全工程に適用するか、節目のみ人間確認を残すか | 節目のみ人間確認とする。テスト工程完了→デプロイ工程移行の前のみユーザーが成果物を確認する。それ以外の工程間移行はレビューサブエージェントの指摘ゼロ判定で自動的に進めてよい |
| 2026-09-23 | セットアップ | 自律ループモードの承認チェックリスト：再レビュー上限到達時、残存指摘が全て軽微なら自動的に次工程へ進めてよいか | 既定通り自動続行してよい |
| 2026-09-23 | セットアップ | 自律ループモードの承認チェックリスト：開発・レビューサブエージェントの進捗報告の頻度 | 1つの工程の作業時間が2時間を超えた場合にのみ報告する |
| 2026-09-23 | セットアップ | .NET 10 SDKが未導入だった（このPCには8.0.408・9.0.314のみ導入済み）。どう進めるか（ユーザー自身が先に導入／.NET 9で代用） | ユーザー自身が公式インストーラーで.NET 10 SDKを先に導入してから進める |
| 2026-09-23 | セットアップ | Playwrightテストの記述言語をどちらにするか（Playwright for .NET／C#、または Playwright Test／Node.js・TypeScript） | Playwright for .NET（C#、NUnitベース）を採用。バックエンドと同じ言語で統一する |
| 2026-09-23 | 関数設計（COMP-01） | コンポーネント設計書4.2節で「送信ボタン押下後の画面反映の要否・具体的な表示内容は関数設計工程で確定する」とされていた点をどう決めるか（独自判断） | 送信結果（成功/失敗）をFUNC-05（displayMessage）で画面に表示する設計とした。読み込み時の未存在メッセージと表示機構を共用でき、利用者への通信結果フィードバックとしても妥当と判断したため。誤りがあれば後日修正する前提 |
| 2026-09-23 | 関数設計（COMP-01） | サーバーが「対応するJSONファイルが存在しない」ことを示すHTTPステータスコードを何にするか（COMP-02の関数設計が未着手のため未確定） | FUNC-04（fetchFormValues）の設計では暫定的に404を前提とした。COMP-02の関数設計時に異なるステータスコードで確定した場合は、本ドキュメント（docs/03_function_design/COMP-01.md）とCOMP-01実装側を追随して修正する前提とする |
| 2026-09-23 | 関数設計（COMP-01） | DOM要素の受け渡し方式をどうするか（id文字列を関数内部でdocument.getElementById等により解決する方式か、呼び出し元がDOM要素・fetch関数そのものを引数として渡す方式か） | 後者（依存性注入方式）を採用した。テスト時にDOM全体や実ネットワークに依存せずモック要素・モック関数を注入できるようにし、テスト容易性を優先したため |
| 2026-09-23 | 関数設計（COMP-02） | 対応するJSONファイルが存在しない場合のHTTPステータスコードをどう確定するか（COMP-01 FUNC-04が暫定404と仮定していた点の確定） | 404 Not Found（本文なし）に確定した。GET対象の資源が存在しない場合のHTTPの標準的な意味付けに合致し、COMP-01 FUNC-04が既に404を前提として実装されていることとも一致するため、COMP-01側の追随修正は不要と判断した。誤りがあれば後日修正する前提 |
| 2026-09-23 | 関数設計（COMP-02） | POST成功時・検証エラー時・想定外エラー時のHTTPステータスコードをどう確定するか（要件定義書・コンポーネント設計書に明記なし） | 成功時200（本文なし）、リクエストボディのJSON構文エラーおよびCOMP-03の検証エラー（success:false）は400、COMP-03側の未処理例外等は500に確定した。独自判断。誤りがあれば後日修正する前提 |
| 2026-09-23 | 関数設計（COMP-02） | コンポーネント設計書5.4節で「関数設計工程への申し送り事項」とされていた排他制御・ロックの要否と方式をどう確定するか | 対応する、と判断した。COMP-02のHTTPハンドラ層でユーザー識別文字列単位の排他ロック（プロセス内メモリ上のSemaphoreSlim等、読み書きを区別しない単純な排他ロック）をCOMP-03への処理委譲区間に適用する方式とした。COMP-03を外部ライブラリ非依存・単体テスト可能な純粋ロジックに保つ方針（NFR-06）を優先し、ロック管理はHTTPリクエストの並行処理を意識するCOMP-02側に置いた。詳細はdocs/03_function_design/COMP-02.md 4節参照。誤りがあれば後日修正する前提 |
| 2026-09-23 | 関数設計（COMP-02） | ユーザー識別用Cookieの名前をどう確定するか（要件定義書・コンポーネント設計書に明記なし） | `lp_user_id`に確定した。他コンポーネントの設計判断に影響しない実装細部のため独自判断とした。誤りがあれば後日修正する前提 |
| 2026-09-23 | 関数設計（COMP-02） | NFR-02（ローカルホスト限定）の解釈をCOMP-05（FUNC-43の固定ポート待受ニーズ）とどう整合させるか（v1.5での修正） | NFR-02が禁止するのはlocalhost以外のホスト・外部ネットワークインターフェースへのバインドであり、ホストをlocalhostに固定したままのポート明示指定（`--urls`/`ASPNETCORE_URLS`）はこれに抵触しないと解釈し、COMP-05のテスト自動化に必要な固定ポート指定を許容する方針に変更した。詳細はdocs/03_function_design/COMP-02.md v1.5改訂履歴・2.1節参照。誤りがあれば後日修正する前提 |
| 2026-09-23 | 関数設計（COMP-03） | POSTされた入力値（REQ-03）の具体的な検証ルール（数値範囲等）をどう確定するか（要件定義書・コンポーネント設計書に明記なし） | `text`は必須・最大1000文字、`select`は必須（空文字列不可）・最大200文字、`radio`は`null`許容・非null時最大200文字、`slider`は有限数値（`NaN`/`±Infinity`不可）であることのみ検証し、具体的な数値範囲やプルダウン/ラジオボタンの選択肢実在チェックは行わない設計とした。選択肢一覧はCOMP-01のHTMLマークアップに属する情報であり、COMP-03が保持するとUI変更のたびに追随修正が必要になりNFR-06の層分離方針に反するため。詳細はdocs/03_function_design/COMP-03.md FUNC-20参照。誤りがあれば後日修正する前提 |
| 2026-09-23 | 関数設計（COMP-03） | ユーザー識別文字列からファイルパスへの変換方式（パストラバーサル等の不正な文字列への対策）をどう設計するか（コンポーネント設計書で「対応付けを含む」とのみ記載、具体方式は未確定） | ユーザー識別文字列をSHA-256でハッシュ化し、16進数64文字固定長の文字列をファイル名とする方式を採用した。入力文字列がそのままパスへ埋め込まれないため、パストラバーサル文字列・OS予約デバイス名・制御文字・極端な長さの文字列のいずれについても安全なファイル名に正規化される。詳細はdocs/03_function_design/COMP-03.md FUNC-19参照。誤りがあれば後日修正する前提 |
| 2026-09-23 | 関数設計（COMP-03） | 保存先ディレクトリ（baseDirectory）をCOMP-02の契約（deps.saveFormData/loadFormDataが2引数のみ）とどう整合させるか | COMP-03側の関数（SaveFormData/LoadFormData）はbaseDirectoryを明示的な引数として受け取る設計とし（テスト時に一時ディレクトリを注入できるようにするため）、COMP-02の2引数契約との差分は実装工程のDI結線コード（アプリ起動時の構成処理）側でクロージャ化して吸収する方針とした。詳細はdocs/03_function_design/COMP-03.md 2.1節参照。誤りがあれば後日修正する前提 |
| 2026-09-23 | 関数設計（COMP-04） | Playwrightがブラウザ上のUIコントロールをどう特定するか（コンポーネント設計書・COMP-01関数設計書のいずれもHTML属性を規定していない） | `data-testid`属性による要素特定方式に確定した。テキストボックス・スライダー・プルダウン・ラジオボタン各選択肢・送信ボタン・読み込みボタン・メッセージ表示領域それぞれに対応する`data-testid`をCOMP-01のHTML実装時に付与する必要があることを申し送る。詳細はdocs/03_function_design/COMP-04.md 2.1節・4節`FormTestIds`参照。誤りがあれば後日修正する前提 |
| 2026-09-23 | 関数設計（COMP-04） | スライダーのmin/max、プルダウン・ラジオボタンの具体的な選択肢値をどう扱うか（COMP-01のHTML実装が未着手で未確定） | `FormFieldLimits`にプレースホルダ値（スライダーmin=0/max=100仮値）を置く設計とし、COMP-01の実HTML属性が確定次第、本ファイルの値を実装値に合わせて更新することを申し送る。詳細はdocs/03_function_design/COMP-04.md 2.1節・4節参照。誤りがあれば後日修正する前提 |
| 2026-09-23 | 関数設計（COMP-04） | テストケースのデータ構造をどう設計するか（往復一致ケースだけでなく、COMP-03の検証エラーにより保存が拒否される境界値ケースも表現できる必要がある） | `FormValuesTestCase`に`ExpectedSubmitSuccess: bool`と`ExpectedRestored: FormValues?`を持たせ、検証エラーを期待するケースは`ExpectedRestored: null`（保存されないため復元時は「未存在」を期待）で表現する設計とした。`select`/`radio`はHTMLの閉じた選択肢集合からのみ値を取るため、これらの境界値は文字数超過ではなく「選択肢の網羅」を採用した。詳細はdocs/03_function_design/COMP-04.md 3節・FUNC-29参照。誤りがあれば後日修正する前提 |
| 2026-09-23 | 関数設計（COMP-04） | テストケースの命名・識別方法をどう設計するか（要件定義書・コンポーネント設計書に明記なし） | `FormValuesTestCase.CaseId`をNUnitのテストケース表示名として用いる設計とした（`TestCaseData.SetName(caseId)`）。障害発生時にどの入力パターンが失敗したかをテスト結果から即座に判別できるようにするため。詳細はdocs/03_function_design/COMP-04.md 2.1節参照。誤りがあれば後日修正する前提 |
| 2026-09-23 | 関数設計（COMP-04） | COMP-04（テストコード側の静的データ・ヘルパー関数）に排他制御が必要か（COMP-02関数設計でCOMP-02側の排他制御は確定済みだが、COMP-04自体の要否は未確定） | 不要と判断した。COMP-04はテストコード側の静的データ・ヘルパー関数のみで構成され、複数テスト間で共有される可変状態を持たない（各テストは独立したブラウザコンテキスト・独立したユーザー識別Cookieを用いる前提であり、これはCOMP-05側の責務）。したがってコンポーネント設計書5.4節の排他制御検討はCOMP-04には適用されないと結論づけた。詳細はdocs/03_function_design/COMP-04.md 2.1節参照。誤りがあれば後日修正する前提 |
| 2026-09-23 | 関数設計（COMP-05） | バックエンドサーバー（COMP-02）プロセスの起動主体をどうするか（CLAUDE.mdが`dotnet test`の標準形のみでのテスト実行を求めており、手動でのサーバー事前起動を前提にできない） | COMP-05自身のテストフィクスチャの`[OneTimeSetUp]`/`[OneTimeTearDown]`でサーバープロセスを起動・終了する設計とした。既存の`GET /api/form-data`エンドポイントへのポーリングで起動完了を待ち受け、COMP-02へ新規ヘルスチェックエンドポイントの追加を要求しない方式とした。詳細はdocs/03_function_design/COMP-05.md 2.1節・FUNC-43/44参照。誤りがあれば後日修正する前提 |
| 2026-09-23 | 関数設計（COMP-05） | サーバーのベースURL・待受ポート番号をどう扱うか（COMP-02関数設計がNFR-02をKestrel既定設定で充足するとのみ定め、具体的なポート番号までは未確定） | プレースホルダとして`http://localhost:5080`を暫定値とし、実装工程でCOMP-02の実際の待受ポートが確定次第更新する方針とした。詳細はdocs/03_function_design/COMP-05.md 2.1節・4節`ServerConfig`参照。誤りがあれば後日修正する前提 |
| 2026-09-23 | 関数設計（COMP-05） | ブラウザの種類・ヘッドレス設定の既定値をどうするか（要件定義書・コンポーネント設計書に明記なし） | Chromiumを既定ブラウザとし、既定で`Headless: true`とした（自動実行時の安定性・CON-01のローカル実行前提を優先）。デバッグ時の切替要否・方式は本工程では規定せず実装工程へ申し送る。詳細はdocs/03_function_design/COMP-05.md 2.1節参照。誤りがあれば後日修正する前提 |
| 2026-09-23 | 関数設計（COMP-05） | シナリオ1（送信→保存検証）における「サーバー保存内容の検証」をどう実現するか（COMP-05はCOMP-01を通じたE2E検証に徹する方針で、COMP-04もHTTPクライアント等の直接検証手段を提供しない） | COMP-01 FUNC-07が表示するメッセージ種別（COMP-04 FUNC-42の`MessageState.Type`）による間接検証（成功メッセージ＝保存成功、エラーメッセージ＝保存拒否）とした。保存内容の値レベルでの一致検証は読み込み経由でのみ確認可能なため、シナリオ2（FUNC-48）の責務とする整理にした。詳細はdocs/03_function_design/COMP-05.md 2.1節・FUNC-47参照。誤りがあれば後日修正する前提 |
| 2026-09-23 | 関数設計（COMP-05） | シナリオ2（読込→復元検証）の前提データをシナリオ1の実行結果から流用するか、シナリオ2自身で独自に用意するか | シナリオ2自身の中で独自に送信操作を行い前提データを用意する設計とした。NUnitのテストケースは実行順序に依存せず独立して成立すべきという一般原則に従う。詳細はdocs/03_function_design/COMP-05.md 2.1節・FUNC-48参照。誤りがあれば後日修正する前提 |
| 2026-09-23 | 要件定義（NFR-02改訂） | 関数設計工程（COMP-02、COMP-05との整合性検討）で判明したNFR-02「既定の設定で」の解釈の曖昧さを、要件定義書側にどう反映するか | 要件定義書をv1.3に改訂し、NFR-02の禁止対象を「ホストをlocalhost以外（`0.0.0.0`やワイルドカード指定等）に変更すること」に明確化し、ホストをlocalhostに固定したままのポート明示指定（テスト自動化目的を含む）は許容されることを明記した。これにより関数設計書（COMP-02、2026-09-23付QA記録参照）の解釈と要件定義書の字面を一致させた |
| 2026-09-23 | 要件定義（NFR-02再改訂・v1.3再承認レビュー対応） | v1.3改訂に対する独立レビューの指摘3件（重大1件・軽微2件）にどう対応するか | 要件定義書をv1.4に改訂し、(1)重大指摘（「ホストがlocalhost固定」という結果ベースの記述に緩めたことで、元の「既定の設定で待ち受ける」が持っていた誤設定防止の防御層が失われていた点）への対応として、サーバー起動時に実バインドアドレスがループバックアドレス（127.0.0.1または::1）のみであることを確認できる手段（起動ログ出力等）を設ける旨をNFR-02に追記（実装方式の詳細はコンポーネント設計・関数設計工程に委ねる旨も付記）、(2)軽微指摘（「テスト自動化等の目的で」という特定用途の例示が実装非依存性を損なう点）への対応として当該例示を削除し「目的を問わず」に一般化、(3)軽微指摘（「localhost（127.0.0.1）」の表記がIPv4のみを指すように読め、IPv6ループバック(::1)への言及がない点）への対応として「127.0.0.1／IPv6の場合は::1を含むループバックアドレス」と明記、の3点を反映した |
| 2026-09-23 | 関数設計（COMP-02） | NFR-02「既定の設定で」の解釈：ホストをlocalhostに固定したままのポート明示指定（`--urls`/`ASPNETCORE_URLS`）をどう解釈するか（独自判断） | 外部公開の防止という本質的要求を損なわない限り許容される、と判断した。その後の要件定義書NFR-02改訂（v1.3→v1.4）で同様の解釈を明記し、要件↔設計の整合が取れた。詳細はdocs/03_function_design/COMP-02.md 2.1節参照 |
| 2026-09-23 | 関数設計（COMP-02） | `--urls`/`ASPNETCORE_URLS`のホスト部分へのワイルドカード指定（`+`・`*`等）をどう扱うか（独自判断） | ワイルドカード指定を用いないことを実装工程で確認すべき事項として新設した。詳細はdocs/03_function_design/COMP-02.md 2.1節参照 |
| 2026-09-23 | 運用ルール（セッション再開時のユーザー指示） | トークン上限に2回到達したため、今回のセッションでの完成を優先したい。レビュー回数をどう扱うか | ユーザー指示: 各成果物に対するレビューは1回に制限して先に進める。後日再レビューを行うため、1回制限で進めた成果物とその回数を記録しておく（`docs/review_log.md`の「レビュー回数制限下で進めた成果物」表に記録）。CLAUDE.mdの再レビュー上限5回の合意は本セッションに限り本指示で上書きする |
| 2026-09-23 | 関数設計（COMP-06・可読性） | 上記指示の下で、COMP-06の可読性向上フェーズ（未着手）をどう扱うか（独自判断） | 論理面は3ラウンドのレビューで収束済みであり、可読性向上は内容変更を伴わないため、完成優先の指示に従い後日の再レビュー時に実施するものとして保留した（`docs/review_log.md`の保留一覧に記録）。関数設計工程はCOMP-06v1.3をもって完了扱いとし実装工程へ進む |
| 2026-09-23 | 実装・テスト | nuget.org（公式）からのNuGetパッケージ（NUnit, NUnit3TestAdapter, Microsoft.NET.Test.Sdk, Microsoft.Playwright.NUnit）の取得をClaudeが`dotnet restore/build`で実行してよいか | 実行してよい（テンプレート既定のパッケージ〈coverlet.collector、NUnit.Analyzers〉および推移的依存の取得を含む。取得したパッケージとライセンスはREADMEの一覧参照） |
| 2026-09-23 | テスト | Playwright用ブラウザ本体（Chromium）のダウンロード・導入（`playwright.ps1 install chromium`）を誰が行うか（グローバルルール上はユーザー実施が原則） | 今回に限りClaudeに実行を許可 |
| 2026-09-23 | 実装（COMP-01/04） | 送信・読み込みボタンのクリック後、非同期処理（fetch→DOM更新）の完了をテストがどう待つか（関数設計書に定めなし。FUNC-42/41を直後に呼ぶと競合状態になる）（独自判断） | COMP-01のform要素（`data-testid="form"`）に`data-request-count`属性を設け、FUNC-07/08の処理完了ごとに1増やす実装とした。COMP-04のFUNC-38/39はクリック前の値を記録し、Playwrightの自動リトライ付きアサーション（`Expect(...).Not.ToHaveAttributeAsync`）で値の変化を待つ。固定時間のsleepを避けるため。`FormTestIds.Form`を追加 |
| 2026-09-23 | 実装（COMP-04） | FUNC-32（スライダー設定）の2方式（`EvaluateAsync`＋イベント発火／`FillAsync`）のどちらに確定するか、および`FormFieldLimits`のプレースホルダ値・選択肢値をどう確定するか（独自判断） | `EvaluateAsync`方式に確定。スライダーmin/maxはHTML実装値（0/100）と一致するため値は据え置き。選択肢値はHTML実装（select: apple/banana/cherry、radio: red/green/blue）に合わせ`FormOptionValues`クラスとして追加した。要素特定は`page.GetByTestId`（既定でdata-testid属性を参照）を用いた |
| 2026-09-23 | 実装（COMP-02/05） | 待受ポート・起動方式・NFR-02の起動時確認手段をどう確定するか（独自判断） | ポートは`http://localhost:5080`に確定（launchSettings.jsonおよびURL未指定時の既定値）。起動後に`IServerAddressesFeature`の全アドレスをログ出力し、ループバック（localhost/127.0.0.1/::1）以外を含む場合は直ちに停止する`LoopbackBindingCheck`を実装した。`AllowedHosts`もループバック名に限定した。E2E（FUNC-43）は`dotnet run --project src/LearnPlaywright.Server --no-build --no-launch-profile --urls http://localhost:5080`で起動し、テストプロジェクトからサーバープロジェクトへ`ReferenceOutputAssembly=false`の参照を張ってビルド順を保証した |
| 2026-09-23 | 実装（COMP-02/03/05） | 保存先ディレクトリ（baseDirectory）の具体値と、E2Eテスト時の保存先をどうするか（独自判断） | 既定はサーバーのContentRoot配下`App_Data/`（`.gitignore`で除外、CON-07）。設定キー`DataDirectory`で上書き可能とし、E2Eテストでは一時ディレクトリを指定してテスト終了時に削除する。Cookieは設計の暫定値（HttpOnly・Path=/・MaxAge 1年）で確定し、`SameSite=Lax`を追加した。ヘッドフル実行の切替は環境変数`LEARNPLAYWRIGHT_HEADED=1`とした |
| 2026-09-23 | テスト | テストの構成（単体テストの対象範囲、COMP-01のJavaScriptの検証手段）をどうするか（独自判断） | COMP-02/03はNUnitの単体テスト（`tests/LearnPlaywright.UnitTests`）、COMP-01〜05の結合はPlaywright E2E（`tests/LearnPlaywright.Tests`）で検証する。Node.js未導入のためCOMP-01のJavaScript関数単体テストは行わず、E2E経由で検証する（テスト仕様書に明記）。TDDは採用せず設計書ベースの後付けテスト、リグレッションはフルリグレッション（`dotnet test`で全件）とした |
| 2026-09-23 | 実装（COMP-01、実装レビュー指摘#8） | 保存データの`radio`が`null`（未選択で保存）のとき、読み込み時に画面上の既存の選択を保持する設計（FUNC-02）では、画面で別の選択をしてから読み込むと保存状態が復元されない。REQ-05の解釈をどうするか（独自判断。レビューはユーザー判断を推奨したが、完成優先の指示のため独自判断し、テスト工程完了時のユーザー確認で見直せるよう記録する） | 案A（`null`の場合は全ラジオボタンの選択を解除して「未選択」を復元する）を採用した。REQ-05の「ラジオボタンの選択項目の復元」に保存時の未選択状態も含むと解釈するのが自然なため。COMP-01関数設計書をv1.3に改訂し、E2E-05を追加した。選択肢に存在しない値（select/radio）は従来どおり現在の選択を変更しない |
| 2026-09-23 | 実装（COMP-02、実装レビュー指摘#5） | 他サイトからのCORSプリフライト不要な単純リクエスト（text/plain）によるPOSTでゴミファイルが作られ得る点にどう対処するか（独自判断） | `Content-Type: application/json`以外のPOSTを415、64K文字超のボディを413で拒否する実装とした（FUNC-18のアダプタ層）。E2E-07を追加 |
| 2026-09-23 | テスト工程完了時のユーザー確認 | 成果物（画面の見た目・教材）の確認結果 | ユーザー回答: 見た目を確認し、教材として問題ない。ただし今回一部の環境構築（NuGetパッケージ取得・Playwright用Chromium導入等）をClaude Codeに任せたため、学習者が自身で実行できるように教材に含めること。→ 教材0章に「手順5: サンプルアプリ（このリポジトリ）を準備する」（リポジトリ取得・ビルド・初回メッセージの説明・起動確認・完成版テスト実行）を追加し、COMP-06関数設計書をv1.4に改訂した |
| 2026-09-23 | 運用ルール（用語） | 「デプロイ」という用語について（ユーザーの懸念: GitHubにソースコードを公開するだけで、サーバーに実行可能な状態で公開するつもりはない） | 本プロジェクトの最終工程は従来からGitHubへのソースコード公開のみを指していたことを確認したうえで、誤解を避けるため工程名を「デプロイ工程」から「公開工程（GitHubへのソースコード公開）」に改め、手順書フォルダを`docs/05_publication/`とした（CLAUDE.md・handoff・テスト仕様書・トレーサビリティを更新）。要件定義書CON-01・スコープ外の「外部サーバーへの本番デプロイは行わない」という否定の記述はそのまま残した |
| 2026-09-23 | テスト→公開工程のゲート | 公開工程（GitHubへのソースコード公開）へ進めてよいか | 承認（「先に進めます」） |
| 2026-09-23 | 公開 | push前スキャンで全92コミットのcommitterに個人メールアドレス（Gmail）を検出。履歴書き換えは禁止のため、公開用の新しい履歴を作る／そのまま公開／公開保留のどれにするか | 公開を保留する。対応案は`docs/05_publication/publication_manual.md` 3章に記録 |
| 2026-09-23 | 公開 | 追跡ファイル中のこのPCの具体的な利用内容を記した記述（CLAUDE.md 2か所、COMP-02.md 1か所）をどうするか | 中立な表現（機微な情報も扱う個人の開発PC）に置き換える。なお既存コミットの履歴には旧記述が残るため、公開方式の決定時に考慮する |
| 2026-09-23 | 公開 | GitHubのリポジトリ名 | `learn-playwright-dotnet` |
| 2026-09-23 | 公開 | ライセンス | MIT。著作権者表記は個人名を避け「learn-playwright-dotnet contributors」とした（独自判断） |
| 2026-09-23 | 公開（ライセンス） | MITライセンスを選んだ場合、プロジェクトで使っている他ツールとの依存関係で問題は発生しないか（ユーザーからの質問） | 問題なしと回答。ローカルNuGetキャッシュのnuspecで確認した直接・推移的依存（NUnit系、Microsoft.NET.Test.Sdk、coverlet.collector、Microsoft.Playwright/.NUnit/.TestAdapter、Microsoft.Bcl.AsyncInterfaces、System.ComponentModel.Annotations）はすべてMIT、.NET本体もMIT。Playwrightパッケージ同梱のドライバはApache-2.0（MITのプロジェクトからの利用に支障なし）。リポジトリは自作ファイルのみで依存物を再配布しないため、依存側の表示義務も生じない。ビルド成果物（bin等）を配布する場合のみApache-2.0のLICENSE/NOTICE同梱が必要になる。READMEに依存関係とライセンスの一覧を追記した |
| 2026-09-23 | 実装（事後報告・セッションレビュー指摘） | 初回の`dotnet new`実行時に生じたプロジェクト外の副作用をどう扱うか | 事前承認なしに次の2点が生じた事実を記録し、ユーザーに報告した。(1) ASP.NET Core HTTPS開発証明書がユーザー証明書ストアに自動インストールされた（信頼登録〈`--trust`〉は未実施、本アプリはHTTPのみで不使用）。(2) .NET CLIのテレメトリ（既定で有効、利用状況をMicrosoftへ送信）が動作した。証明書の削除（`dotnet dev-certs https --clean`）・テレメトリの無効化（環境変数`DOTNET_CLI_TELEMETRY_OPTOUT=1`）はユーザーの判断・実行に委ねる（Claudeは実行しない） |
| 2026-09-23 | テスト（事後報告・セッションレビュー指摘） | Chromium導入時の`-ExecutionPolicy Bypass`指定について | 承認を得たのはChromiumの導入のみで、pwsh未導入時にWindows PowerShellへ`-ExecutionPolicy Bypass`を付けて実行することは事前に説明していなかった（実行後に報告）。効果は当該プロセス1回限りで、システムの実行ポリシーは変更していない。以後、同種の実行制限緩和は事前に目的・影響を示して承認を得る |
| 2026-09-23 | Git規約（全プロジェクト共通） | Claudeのコミットのauthorを`ClaudeCode`にしていたため、ユーザーのリポジトリの大半でユーザーの貢献が記録されない。今後のコミットをどうするか | git-conventionsスキルを改訂: 今後はauthor・committerともユーザー本人（`--author`を指定せずGit設定を使う）。Claudeの関与は`[claude]`接頭辞（維持）と`Co-Authored-By`トレーラーで示す。`user.email`はユーザー自身がGitHubのnoreplyアドレスに設定し、Claudeはコミット前に確認してnoreply以外なら報告して止まる || 2026-09-23 | Git規約（全プロジェクト共通） | 過去の履歴から削除したい情報の除去やauthor修正を、一時的に権限を持つサブエージェントで行えるか。行う場合の実行者と、フルバックアップの方法 | 禁止リスト（deny）と規約はサブエージェントにも適用されるため、権限を一時付与する方式はとらない。新スキル`git-history-rewrite`を作成: 書き換えとforce pushはユーザーが`!`付きで実行し、Claudeは調査・フルバックアップ（`git bundle`＋tarによるzip＋ハッシュ値、push済みなら`clone --mirror`、保存先はリポジトリ外かつクラウド同期外）・指示ファイル作成・検証・復元案内を担当する |

## Pushスキャン記録

GitHubへのgit push前に実施した機密情報・個人情報スキャンの結果をpushごとに記録する（CLAUDE.md「デプロイ（GitHub公開）」章参照）。

| 日付 | 対象コミット/ブランチ | スキャン範囲 | 検出事項 | 対応 |
|---|---|---|---|---|
| 2026-09-23 | master（初回〜8cb6f8a、全92コミット） | 追跡ファイル50件の内容、全コミットのauthor・committer、ignored状態 | 内容: 秘密情報なし。PC利用内容の記述3か所。履歴: committerに個人メールアドレス（全コミット）、旧記述も履歴に残存 | 記述は中立化。メールアドレスの件によりpushせず公開保留（ユーザー判断） |
