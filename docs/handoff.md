# 申し送り事項

## 現在の申し送り

- **トークン上限接近のためユーザー指示によりセッション中断（2026-09-23）**。未コミットの変更はなし（`af8a5ea`まで全てコミット済み）。
- **再開ポイント**: 関数設計工程、COMP-04（テストデータ構造・ヘルパー関数）の論理設計ドラフト作成済み（`docs/03_function_design/COMP-04.md` v1.0、コミット`af8a5ea`）。中断指示後に論理レビューサブエージェントの結果が届いたため、`docs/review_log.md`にラウンド1の指摘（重大2件・軽微2件、コミット`c3b0191`）として記録済み。**次回セッションでは、この4件の指摘を開発サブエージェントに差し戻して修正するところから再開する**（レビューのやり直しは不要）。指摘内容の要点:
  1. （重大）FUNC-30〜42の対応要件から誤って付与されたREQ-09を削除する（REQ-09はCOMP-05専属）
  2. （重大）2.1節が「qa_log.mdに記録する」と自己申告している独自判断のうち「テストケースの命名・識別方法」「排他制御の要否」の2件が実際には`docs/qa_log.md`に未記載のため追記する
  3. （軽微）3節に`FormValuesTestCases`静的クラスのコード例を追加する
  4. （軽微）6節自己チェック見出しにCON-07を追加する
- 完了済み: 要件定義（v1.2）、コンポーネント設計（v1.3、COMP-01〜06）、関数設計のうちCOMP-01（v1.2）・COMP-02（v1.4）・COMP-03（v1.3）が完了（論理・可読性とも収束）。トレーサビリティマトリックス②表はFUNC-01〜42まで反映済み。
- 残工程: 関数設計のCOMP-04（レビューからやり直し）・COMP-05（Playwrightテストシナリオ）・COMP-06（学習教材）が未着手/未完了。その後、実装（`src/`）・テスト（`docs/04_test/`・`tests/`）・デプロイ（GitHub Public公開、テスト完了時に一度ユーザー確認が必要）が残っている。
- COMP-04設計時の独自判断（`docs/qa_log.md`参照、2026-09-23）: data-testid属性方式の採用（COMP-01実装時にHTML側への対応属性追加が必要）、スライダーmin/max・プルダウン/ラジオの選択肢値はCOMP-01実装確定後にCOMP-04.mdを更新する必要がある、select/radioの境界値は選択肢網羅方式を採用。実装工程着手時に見落とさないよう注意。

## 過去の申し送り（解決済み）

- **.NET 10 SDKの導入待ち**: 2026-09-23、ユーザーが公式インストーラーで.NET 10 SDK（10.0.401）を導入完了。`dotnet --list-sdks`で8.0.408・9.0.314・10.0.401の3系統が確認できる状態。実装工程着手時に`global.json`で`10.0.x`に固定する。
