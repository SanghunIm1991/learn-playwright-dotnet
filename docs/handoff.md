# 申し送り事項

## 現在の申し送り

- **.NET 10 SDKの導入待ち**: このPCには2026-09-23時点で.NET 8.0.408・9.0.314のみ導入済み。ユーザーが公式インストーラー（https://dotnet.microsoft.com/download/dotnet/10.0 ）で.NET 10 SDKを導入するまで、`dotnet`コマンドを用いる実装・テスト工程（`src/`・`tests/`のビルド・実行確認）には着手できない。要件定義・コンポーネント設計・関数設計（ドキュメント作成工程）は導入を待たずに並行して進めてよい。
- 導入完了後、`dotnet --list-sdks`で`10.0.x`が表示されることを確認し、`global.json`で`10.0.x`に固定してから実装工程に着手する。

## 過去の申し送り（解決済み）

（なし）
