# 申し送り事項

## 現在の申し送り

- **トークン上限接近のためユーザー指示によりセッション中断（2026-09-23）**。未コミットの変更はなし（`529b6db`まで全てコミット済み）。
- **再開ポイント**: 関数設計COMP-06（学習教材ドキュメント）の論理設計フェーズが3ラウンドのレビューを経て収束済み（v1.3、コミット`2e08ab3`）。**次回セッションでは、COMP-06の可読性向上フェーズ（可読性向上サブエージェント→可読性レビューサブエージェント）から再開する**（COMP-01〜05と同じパターン。`docs/03_function_design/COMP-01.md`〜`COMP-05.md`の可読性向上プロンプトを参考にできる）。可読性向上が収束すれば、関数設計工程（COMP-01〜06）は全て完了となる。
- 完了済み: 要件定義（v1.4、COMP-02/05の関数設計工程で判明したNFR-02の解釈曖昧さを解消するため改訂済み）、コンポーネント設計（v1.3、COMP-01〜06）、関数設計のうちCOMP-01（v1.2）・COMP-02（v1.6）・COMP-03（v1.3）・COMP-04（v1.2）・COMP-05（v1.3）・COMP-06論理面（v1.3）が完了。トレーサビリティマトリックス②表はFUNC-01〜58まで反映済み。
- 残工程: 関数設計COMP-06の可読性向上フェーズ、その後、実装（`src/`）・テスト（`docs/04_test/`・`tests/`）・デプロイ（GitHub Public公開、テスト完了時に一度ユーザー確認が必要）が残っている。
- 特記事項（実装工程で見落とさないよう注意）:
  - COMP-01設計時の独自判断（`docs/qa_log.md`参照）: data-testid属性方式の採用（COMP-01実装時にHTML側への対応属性追加が必要）、スライダーmin/max・プルダウン/ラジオの選択肢値はCOMP-01実装確定後にCOMP-04.mdを更新する必要がある、select/radioの境界値は選択肢網羅方式を採用。
  - COMP-02のサーバー起動ポート方式: `--urls`/`ASPNETCORE_URLS`でホストをlocalhostに固定した上でのポート明示指定を採用（要件定義書NFR-02 v1.4で正式に許容）。ワイルドカードホスト指定（`+`/`*`）は禁止。起動時にバインドアドレスがループバックのみであることを確認する手段（起動ログ出力等）も要件化済み（NFR-02）。
  - COMP-05のFUNC-43はCOMP-02サーバープロセスを自身で起動する設計。学習教材COMP-06ではFUNC-51〜56は読者が`dotnet run`で手動起動、FUNC-57以降はCOMP-05による自動起動に切り替わる（教材内で明示する設計）。

## 過去の申し送り（解決済み）

- **.NET 10 SDKの導入待ち**: 2026-09-23、ユーザーが公式インストーラーで.NET 10 SDK（10.0.401）を導入完了。`dotnet --list-sdks`で8.0.408・9.0.314・10.0.401の3系統が確認できる状態。実装工程着手時に`global.json`で`10.0.x`に固定する。
- **関数設計COMP-04の論理レビュー指摘対応**: 2026-09-23、REQ-09の誤付与削除・qa_log記録漏れ・コード例追加・見出し修正の4件を対応し収束（v1.2）。
- **COMP-02/COMP-05間のサーバーポート指定に関する設計矛盾**: 2026-09-23、COMP-05のテスト自動化に必要な固定ポート指定と、COMP-02のNFR-02解釈（設定変更を行わない）が矛盾していることが判明。要件定義書NFR-02をv1.3→v1.4で改訂（ホストをlocalhostに固定した上でのポート明示指定を許容、ワイルドカードホスト禁止、起動時のバインドアドレス確認手段を追加）し、COMP-02関数設計書もv1.6まで改訂して整合を確保。一連の経緯は`docs/review_log.md`のコミット`30d3e81`〜`c4e0d6f`周辺に記録。
